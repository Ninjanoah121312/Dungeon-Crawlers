// Point this at wherever the bot/API is hosted on Wispbyte.
// This is the ONLY place that needs editing when you deploy — everything
// else in web/ reads from window.TICKET_DESK_CONFIG.
window.TICKET_DESK_CONFIG = {
  API_BASE_URL: 'https://DungeonCrawlers.wispbyte.app',
};
